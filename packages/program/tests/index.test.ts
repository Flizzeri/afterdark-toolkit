// packages/program/tests/index.test.ts

import { describe, it, expect } from 'vitest';

describe('core package', () => {
        it('should exist', () => {
                expect(true).toBe(true);
        });
});
