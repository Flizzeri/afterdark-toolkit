{
        "name": "@afterdarktk/core",
        "version": "0.1.0",
        "main": "dist/index.js",
        "types": "dist/index.d.ts",
        "scripts": {
                "build": "tsup src/index.ts --dts --format esm,cjs",
                "check": "tsc --noEmit -p tsconfig.json",
                "test": "vitest run",
                "coverage": "vitest run --coverage"
        },
        "type": "module",
        "dependencies": {
                "vitest": "^4.0.8"
        }
}
